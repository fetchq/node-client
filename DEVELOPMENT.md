# Development

First, check that all is ok:

```bash
make start
make test-run
```

## Working on the Examples

### From the root:

```bash
npm install
npm link
```

### From the example project:

```bash
make start
```
